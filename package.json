{
  "name": "workshop-simulator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/simulator.ts",
    "build": "bun build src/simulator.ts --outdir dist --target bun",
    "run:built": "bun dist/simulator.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.55.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
